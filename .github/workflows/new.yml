name: build_push_deploy

on:
  push:
    branches:
     - main



jobs:
  build_push:
    runs-on: ubuntu-latest
    steps:
    - name: add env
      run:
        echo "NEXT_PUBLIC_API_URL=https//dev.lever.io/api/" >> "$GITHUB_ENV"
        cat "$NEXT_PUBLIC_API_URL"
        cat $GITHUB_ENV
